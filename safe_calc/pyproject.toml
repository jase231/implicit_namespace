[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "arith-calc"
version = "0.1.0"
dependencies = [ "safe-math", "cppyy" ]

[project.scripts]
arith_calc = "arith.safe_calc:main"

[tool.hatch.build.targets.wheel]
packages = ["arith"]
