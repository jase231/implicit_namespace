[build-system]
requires = ["scikit-build-core"]
build-backend = "scikit_build_core.build"

[project]
name = "safe_div"
version = "0.0.1"
authors = [
  { name = "Jake Serwe" },
]
requires-python = ">=3.10"

[tool.scikit-build]
wheel.install-dir = "arith"
